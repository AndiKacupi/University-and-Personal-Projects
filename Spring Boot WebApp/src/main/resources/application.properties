spring.datasource.url = jdbc:mysql://localhost:3306/hrm?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=root

#Hibernate
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect

#Hibernate auto ddl
spring.jpa.hibernate.ddl-auto=update

#logging.level.org.hibernate.SQL=DEBUG

#login username and password set by me
#the default username is user and the password is generated and shown in the terminal when we run the app
spring.security.user.name=user
spring.security.user.password=user